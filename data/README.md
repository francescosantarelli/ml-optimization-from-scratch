# Data

The dataset used for the original university assignment is intentionally not included in this repository because redistribution rights are unclear.

The experiment scripts expect a CSV file with numerical feature columns and a target column named `label`, with labels encoded as `-1` and `+1`.
